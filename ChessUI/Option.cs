namespace ChessUI
{
    // Enum representing menu options available to the player
    public enum Option
    {
        // Option to restart the game from the beginning
        Restart,
        // Option to exit and close the application
        Exit,
        // Option to continue playing (not currently used)
        Continue,
    }
}