<Window x:Class="WpfAdminPeritz.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfAdminPeritz"
        mc:Ignorable="d"
        Title="MainWindow" Height="754" Width="661"
        Background="#181818">

    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="HeaderStyle">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="36"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0, 0, 0, 40"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="Button" x:Key="LoginButtonStyle">
            <Setter Property="Background" Value="#65A63F"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="55"/>
            <Setter Property="Width" Value="350"/>
            <Setter Property="Margin" Value="0, 20, 0, 0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Control" x:Key="InputBaseStyle">
            <Setter Property="Height" Value="55"/>
            <Setter Property="Width" Value="350"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Background" Value="#252525"/>
            <Setter Property="BorderBrush" Value="#353535"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="50, 0, 10, 0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0, 10, 0, 10"/>
        </Style>

        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <Style TargetType="TextBox" x:Key="EmailInputStyle" BasedOn="{StaticResource InputBaseStyle}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="5">
                                <ScrollViewer x:Name="PART_ContentHost" Focusable="False" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                            </Border>
                            <Path Data="M20,4H4C2.9,4,2.01,4.9,2.01,6L2,18C2,19.1,2.9,20,4,20H20C21.1,20,22,19.1,22,18V6C22,4.9,21.1,4,20,4Z M20,8L12,13L4,8V6L12,11L20,6V8Z"
                                  Fill="#A9A9A9" Width="20" Height="20"
                                  HorizontalAlignment="Left" VerticalAlignment="Center"
                                  Margin="15, 0, 0, 0"/>
                            <TextBlock Text="Email"
                                       Foreground="#A9A9A9"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Left"
                                       Margin="50, 0, 0, 0"
                                       FontSize="16"
                                       IsHitTestVisible="False"
                                       Visibility="{Binding Text.IsEmpty, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type TextBox}}, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="PasswordBox" x:Key="PasswordInputStyle" BasedOn="{StaticResource InputBaseStyle}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Grid>
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="5">
                                <ScrollViewer x:Name="PART_ContentHost" Focusable="False" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                            </Border>

                            <Path Data="M18,8H17V6C17,3.24 14.76,1 12,1C9.24,1 7,3.24 7,6V8H6C4.9,8 4,8.9 4,10V20C4,21.1 4.9,22 6,22H18C19.1,22 20,21.1 20,20V10C20,8.9 19.1,8 18,8Z M12,17C10.9,17 10,16.1 10,15C10,13.9 10.9,13 12,13C13.1,13 14,13.9 14,15C14,16.1 13.1,17 12,17Z M15,8H9V6C9,4.34 10.34,3 12,3C13.66,3 15,4.34 15,6V8Z"
                                  Fill="#A9A9A9" Width="20" Height="20"
                                  HorizontalAlignment="Left" VerticalAlignment="Center"
                                  Margin="15, 0, 0, 0"/>

                            <TextBlock x:Name="Placeholder" 
                                       Text="Password"
                                       Foreground="#A9A9A9"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Left"
                                       Margin="50, 0, 0, 0"
                                       FontSize="16"
                                       IsHitTestVisible="False"
                                       Visibility="Collapsed"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <DataTrigger Binding="{Binding Password.Length, RelativeSource={RelativeSource Self}}" Value="0">
                                <Setter TargetName="Placeholder" Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="Placeholder" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="400">
            <TextBlock Text="Chess" Style="{StaticResource HeaderStyle}"/>

            <TextBox x:Name="EmailTextBox" Style="{StaticResource EmailInputStyle}"/>

            <PasswordBox x:Name="PasswordBox" Style="{StaticResource PasswordInputStyle}"/>


            <Button Content="Log In" Style="{StaticResource LoginButtonStyle}" Click="Button_Click"/>

            <StackPanel Orientation="Horizontal"
            HorizontalAlignment="Center"
            Margin="0,15,0,0">
                <TextBlock Text="New? "
               Foreground="#A9A9A9"
               FontSize="14"/>

                <TextBlock FontSize="14"
               Foreground="#65A63F"
               FontWeight="SemiBold"
               Cursor="Hand"
               TextDecorations="Underline"
               MouseLeftButtonDown="SignUp_Click">
        Sign up
                </TextBlock>

                <TextBlock Text=" and start playing"
               Foreground="#A9A9A9"
               FontSize="14"/>
            </StackPanel>


        </StackPanel>
    </Grid>
</Window>